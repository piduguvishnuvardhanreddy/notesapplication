# ── Environment variables (NEVER commit these — contains API URLs/keys) ──
.env
.env.local
.env.*.local
.env.production

# ── Dependencies ──
node_modules/

# ── Build output ──
dist/
dist-sse/

# ── Vite cache ──
.vite/

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ── OS files ──
.DS_Store
Thumbs.db

# ── Editor ──
.vscode/
.idea/
